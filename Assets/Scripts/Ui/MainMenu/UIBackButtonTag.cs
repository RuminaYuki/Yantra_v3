using UnityEngine;

/// <summary>
/// แปะบนปุ่มที่ควรใช้เสียง Back แทนเสียง Click
/// เช่น ปุ่ม Back, Resume, Cancel
///
/// เป็นคลาสเปล่า ไม่มีโค้ดอะไร ทำหน้าที่เป็นป้ายชื่อให้ UISoundEmitter เช็ค
/// วิธีนี้ง่ายกว่าการไปตั้งค่าเสียงทีละปุ่ม
/// </summary>
public class UIBackButtonTag : MonoBehaviour
{
}
